<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.clone.mapper.UserMapper">

    <insert id="insertAccount" parameterType="com.naver.clone.dto.UserDto">
        INSERT INTO USER_ACCOUNT (USER_ID, USER_PW)
        VALUES (#{userId}, #{userPw})
    </insert>

    <insert id="insertProfile" parameterType="com.naver.clone.dto.UserDto">
        INSERT INTO USER_PROFILE (
        USER_ID, USER_NAME, EMAIL, BIRTH, TELECOM, GENDER, IS_LOCAL, PHONE
        ) VALUES (
        #{userId}, #{userName}, #{email}, #{birth}, #{telecom}, #{gender}, #{isLocal}, #{phone}
        )
    </insert>

    <select id="findById" parameterType="java.lang.String" resultType="com.naver.clone.dto.UserDto">
        SELECT
            A.USER_ID,
            A.USER_PW,
            P.USER_NAME,
            P.EMAIL,
            P.PHONE,
            P.GENDER
        FROM USER_ACCOUNT A
        JOIN USER_PROFILE P ON A.USER_ID = P.USER_ID
        WHERE A.USER_ID = #{userId} AND A.STATUS = 'Y'
    </select>

    <select id="selectById" parameterType="java.lang.String" resultType="string">
        SELECT USER_ID
        FROM USER_PROFILE
        WHERE PHONE = #{info} OR EMAIL = #{info}
    </select>

    <select id="checkUserExists" parameterType="java.lang.String">
        SELECT COUNT(USER_ID)
        FROM USER_PROFILE
        WHERE USER_ID = #{userId}
    </select>

    <update id="updatePassword" parameterType="java.lang.String">
        UPDATE USER_ACCOUNT
        SET USER_PW = #{newPw}
        WHERE USER_ID = #{userId}
    </update>

    <select id="isUserIdExist" parameterType="java.lang.String">
        SELECT COUNT(USER_ID)
        FROM USER_ACCOUNT
        WHERE USER_ID = #{userId}
    </select>
</mapper>